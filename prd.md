# Bitfinex 放貸市場決策輔助系統 PRD

## 1. 專案背景

Bitfinex 放貸市場利率會隨供需快速變動，單靠人工觀察 funding book 不容易判斷合理掛單利率、等待時間與資金利用效率。

本專案延續原本的資料收集與建模方向，進一步將「如何設定較合理的放貸條件」轉化為可驗證的決策輔助任務。系統會收集 `fUSD`、`fBTC`、`fETH` funding book 資料，建立利率預測、成交機率估計與回測評估流程，協助使用者觀察市場狀態與合理利率區間。

本系統定位為決策輔助原型，不是自動交易或自動放貸系統。

## 2. 產品目標

建立一套 Bitfinex 放貸市場決策輔助系統，能定期收集 `fUSD`、`fBTC`、`fETH` funding book 資料，整理成結構型建模資料，訓練 baseline、線性迴歸、決策樹與 XGBoost 模型，並透過回測評估模型策略與 baseline 策略的差異。

系統最後需能產出：

- 建議觀察利率區間
- 模型預測值與信心提示
- 模擬成交率
- 資金利用率
- 平均等待時間
- 回測與模型評估報告
- 風險提示與人工確認提醒

系統不自動下單、不保證收益、不提供投資建議。

## 3. 使用者

主要使用者：

- 專案作者本人

主要使用情境：

- 想長期追蹤 Bitfinex 放貸市場利率變化
- 想用歷史 funding book 資料判斷市場狀態
- 想了解不同利率區間可能帶來的等待時間與成交機率
- 想比較模型策略與簡單 baseline 策略
- 想用 Demo 簡報說明資料來源、欄位定義、建模流程與使用限制

## 4. 最重要成果

本專案最重要的成果是「不自動下單、需人工確認的放貸決策輔助原型」。

完成後，系統應能從 Bitfinex funding book 歷史資料中產生模型輸入資料，建立利率預測與成交機率估計，並透過回測說明模型策略是否優於 baseline 策略。

## 5. 本次必做範圍

### 5.1 資料來源

系統需使用 Bitfinex 公開 API 取得 funding book 資料。

本次必抓 markets：

- `fUSD`
- `fBTC`
- `fETH`

參考資料：

- Bitfinex Public Books endpoint
- API 範例：`https://api-pub.bitfinex.com/v2/book/fUSD/P0?len=25`

### 5.2 資料收集

系統需能：

- 呼叫 Bitfinex funding book API
- 對三個指定 markets 重複抓取資料
- 記錄每次抓取時間
- 保存 funding book 快照
- 處理 API 請求失敗、格式異常、網路錯誤等狀況
- 保留 crawl log 與 error log

### 5.3 原始資料欄位

每筆 funding book 原始資料至少應整理出：

- market：市場代號，例如 `fUSD`
- rate：利率
- period：期間
- count：彙總筆數
- amount：數量
- side：供給或需求方向，依 amount 正負值判斷
- fetched_at：資料抓取時間

### 5.4 建模資料與特徵

系統需將 funding book 快照整理成可建模的結構型資料。

建模特徵至少包含：

- market
- hour
- day_of_week
- avg_rate
- weighted_avg_rate
- min_rate
- max_rate
- total_amount
- avg_period
- offer_count
- demand_count
- rate_spread
- previous_weighted_avg_rate
- rate_change
- amount_change

第一版預測目標：

- 下一個觀察時間點的加權平均利率或合理利率區間

### 5.5 模型訓練

系統需建立以下模型或策略：

- baseline：平均值預測或前一期利率預測
- 線性迴歸
- 決策樹
- XGBoost

訓練流程需符合 Scikit-learn 標準流程：

- 資料切分
- 模型訓練
- 驗證
- 評估
- 結果解讀

### 5.6 模型評估

模型評估需包含：

- MAE
- RMSE
- R2
- 模型比較表
- 預測值與實際值比較
- 模型限制說明

### 5.7 回測與業務指標

除了模型指標外，系統需建立回測流程，模擬不同策略在歷史資料中的表現。

回測至少需比較：

- baseline 策略
- 模型策略

業務指標至少包含：

- 模擬成交率
- 資金利用率
- 平均等待時間
- 最大連續未成交時間
- 模擬年化收益
- 與 baseline 策略的差異

### 5.8 決策輔助輸出

系統輸出不直接下單，而是提供：

- 建議觀察利率區間
- 模型預測值
- 模型信心或限制提示
- baseline 策略比較
- 模擬成交率與等待時間
- 風險與限制說明
- 人工確認提醒

### 5.9 資料儲存

系統需支援：

- SQLite
- CSV

SQLite 至少需保存：

- funding book 快照
- 建模資料集
- crawl log
- error log
- 模型評估結果
- 回測結果

CSV 至少需可輸出：

- 原始整理資料
- 建模資料集
- 模型評估表
- 回測結果表

### 5.10 文件與 Demo

專案需提供：

- 成果報告
- 操作說明
- 資料來源說明
- 欄位定義
- 建模流程說明
- 模型效能評估報告
- 回測與業務指標報告
- 應用情境 Demo 簡報
- 使用限制與人工確認流程說明

## 6. 本次不做範圍

本次明確不做：

- 自動下單
- 自動放貸
- 自動調整放貸條件
- 使用私人 API key 操作帳戶
- 保證收益
- 直接提供投資建議
- 在模型信心不足或資料不足時輸出操作建議

系統只提供資料分析、模型評估、回測結果與人工判斷輔助。

## 7. 可延後功能

以下功能可作為未來擴充，不列入本次必要完成範圍：

- 網頁儀表板
- 即時通知，例如 Email、LINE、Telegram
- 雲端部署
- 更多 funding markets
- 更完整的資金部位管理
- 串接 Bitfinex authenticated API
- 真實小額模擬交易紀錄
- 自動產生每日或每週報表
- 更完整的市場外部特徵，例如 BTC / ETH 價格波動與成交量

## 8. 完成定義

本專案符合以下條件，即視為本次完成：

1. 可抓取 `fUSD`、`fBTC`、`fETH` funding book 資料。
2. 可將資料保存到 SQLite 與 CSV。
3. 可產生建模資料集。
4. 可明確定義業務問題、建模任務、預測目標與評估指標。
5. 可建立 baseline 模型或 baseline 策略。
6. 可訓練線性迴歸、決策樹與 XGBoost 模型。
7. 可輸出 MAE、RMSE、R2 與模型比較結果。
8. 可執行回測，並比較模型策略與 baseline 策略。
9. 可計算模擬成交率、資金利用率、等待時間等業務指標。
10. 可產出建議觀察利率區間與風險提示。
11. 所有建議均標示為人工確認，不自動下單。
12. 有完整訓練、推論與回測程式碼。
13. 有模型效能評估報告。
14. 有回測與業務指標報告。
15. 有 Demo 簡報，包含欄位定義、資料來源、使用限制與人工確認流程。

## 9. 驗收標準

### 9.1 資料抓取驗收

- 手動執行程式時，三個 markets 都能成功呼叫 Bitfinex API。
- 每個 market 至少能取得一批 funding book 資料。
- 回傳資料能被整理成固定欄位格式。
- API 錯誤或資料格式異常時會留下錯誤紀錄。

### 9.2 儲存驗收

- SQLite 資料庫成功建立。
- 原始整理資料可寫入 SQLite。
- 建模資料集可寫入 SQLite 或輸出 CSV。
- 模型評估結果可保存。
- 回測結果可保存。
- CSV 欄位名稱清楚，可直接開啟檢視。

### 9.3 建模驗收

- 可產生訓練集與驗證集。
- 可建立 baseline。
- 可訓練線性迴歸。
- 可訓練決策樹。
- 可訓練 XGBoost。
- 可輸出 MAE、RMSE、R2。
- 可用表格比較各模型表現。
- 可解釋模型結果與限制。

### 9.4 回測驗收

- 可使用歷史資料模擬 baseline 策略。
- 可使用歷史資料模擬模型策略。
- 可計算模擬成交率。
- 可計算資金利用率。
- 可計算平均等待時間。
- 可計算最大連續未成交時間。
- 可比較模型策略與 baseline 策略差異。

### 9.5 決策輔助驗收

- 可輸出建議觀察利率區間。
- 可顯示模型預測值。
- 可顯示模型信心或限制提示。
- 可顯示風險與使用限制。
- 輸出結果需明確標示「需人工確認」。
- 系統不執行任何真實下單或帳戶操作。

### 9.6 文件驗收

- 操作說明能讓使用者知道如何安裝、執行、訓練模型、執行回測與查看結果。
- 成果報告能說明資料來源、欄位定義、建模任務、模型比較、回測結果與限制。
- Demo 簡報需包含欄位定義、資料來源、應用情境、使用限制與人工確認流程。

## 10. 主要限制與假設

- 系統以本機執行為主。
- Bitfinex 公開 API 可正常存取。
- 本次不處理需要登入或 API key 的私人帳戶資料。
- 本次不自動下單。
- 本次不保證收益。
- 模型結果僅作為資料科學學習與放貸市場觀察輔助。
- funding book 是即時市場資料，若資料收集時間不足，模型與回測結果可能不穩定。
- XGBoost 不一定優於簡單模型，需以驗證集與回測指標判斷。
- 若 Bitfinex API 格式變更，系統需調整解析邏輯。

## 11. 初步開發切分

建議後續開發可分為以下階段：

1. Bitfinex funding book API 探索與資料載入
2. 原始資料整理與 SQLite / CSV 儲存
3. 建模資料集與特徵工程
4. Baseline 策略設計
5. 線性迴歸、決策樹、XGBoost 模型訓練
6. MAE、RMSE、R2 模型評估
7. 回測流程設計
8. 成交率、資金利用率、等待時間等業務指標
9. 決策輔助輸出
10. 成果報告、操作說明與 Demo 簡報

## 12. 一句話摘要

建立一套 Bitfinex 放貸市場決策輔助原型，透過 funding book 歷史資料完成利率預測、成交機率估計與回測評估，輸出建議觀察利率區間與風險提示，但不自動下單，所有結果皆需人工確認。
